version: '2.1'
services:

  newsboat:
    image: "newsboat-copy-data:2.12"
    build: .
    volumes:
      - "${PWD}/data/:/data-to-copy/"
      - "${PWD}/urls:/data/urls"
  fullfeedrss:
    image: "fivefilters-full-text-rss-docker:3.5"
